// prowl-pi — pi extension adapter for Prowl.
// Registers commands and binds core ports to pi tools.
//
// Commands: /prowl search, /prowl query, /prowl chat

export { scrape } from "./firecrawl-client.ts";
export { searxngClient } from "./searxng-client.ts";
