# Prowl v0.1 — Implementation Roadmap

> Strict dependency-ordered feature roadmap from the current (near-empty) TypeScript
> monorepo to a usable local, Pi-first vertical slice.
> **Roadmap only.** This document proposes phases; it writes no code, edits no Docker
> config, installs no packages, and commits nothing.

## Workflow-tag legend (rpiv-pi canonical meanings)

These tags choose the agent workflow to use when a phase is later executed — they do
**not** describe whether a phase has live I/O, whether it is deployed, or whether it
writes ports/types.

- **ship** — a small, low-risk, well-understood change. Fast path; it skips research
  and review. Use for isolated config, docs, or obvious glue after the design is settled.
- **build** — normal feature work. Use for an adapter, a pipeline slice, command
  behavior, or anything needing research/design, implementation, validation, and a review loop.
- **arch** — a consequential architectural or cross-cutting change affecting multiple
  layers, contracts, dependency direction, or a subsystem boundary.

---

## 1. Current State (ground truth, read from disk)

| Area | Status |
| --- | --- |
| `packages/core/src/ports.ts` | 7 ports defined. Missing: `CatalogPort`. `StoragePort` is **not** tenant-scoped (`PRD §11.2`). |
| `packages/core/src/index.ts` | Type-only barrel. No pipeline exports. |
| `packages/pi/src/searxng-client.ts` | Working `SearchPort` (SearXNG JSON API). ✅ |
| `packages/pi/src/firecrawl-client.ts` | Working `scrape()` (`/v1/scrape`, markdown). ✅ |
| `packages/pi/src/config.ts` | `DEFAULT_SCRAPE_OPTIONS` + `ScrapeOverrides`. ✅ |
| `packages/pi/src/index.ts` | **Stub** — re-exports only. No command/tool/hook registration. |
| Missing adapters | `ModelPort` (Qwen), `PresenterPort`, `UserPromptPort`, `StoragePort` (fs), `CatalogPort` (fs). |
| `package.json` `exports` map | Absent in both packages — needed for clean `prowl-core` / `prowl-pi` imports. |
| Tests | None. |
| Pi deploy | `~/.pi/agent/extensions/prowl/` not populated from `packages/pi`. |
| Docker stack | SearXNG (:8888) + Firecrawl (:3002) verified running. `.env` + `docker-compose.yml` present. |

**SearXNG engine availability (verified):** Google, Bing, Yandex, Naver, Mail.ru,
360search, Sogou (JS-rendered), Marginalia, Wiby, Mojeek, Brave, Qwant, Startpage,
DDG, searchmysite. (All configured in `searxng/settings.yml`.)

---

## 2. Guiding Constraints (from PRD / guidance)

- **Hexagonal:** Core = ports + orchestration + ranking/policy. Zero runtime deps; never
  imports Pi, Docker, `fetch`, or terminal APIs. Adapters own I/O and Pi UI.
- **SearXNG = discovery/ranking** (candidate URLs). **Firecrawl = conditional extraction**
  of a *bounded selected set* only — never scrape every result by default.
- **v0.1 local Pi-first, single-user.** Do **not** let tenant/persistence design delay the
  first vertical slice. Filesystem persistence + `CatalogPort` are deferred until after the
  first working `prowl search` demo (see Phase ordering below).
- **Commands:** `/prowl search`, `/prowl query`, `/prowl chat` — composed from core
  primitives (`PLAN`, `SCATTER`, `GATHER`, `READ`, `SYNTHESIZE`, `PRESENT`, `REFLECT`).
  For the first slice, only `search` is required; `query`/`chat` are later phases.
- **Single model (Qwen)** for both planning and synthesis, via OpenAI-compatible endpoint.
- **Firecrawl is conditional with an explicit user trigger:** `prowl search --read <topic>`
  enables evidence mode. Default `search` uses snippets only. Read mode selects a bounded
  set (3–5 diverse, high-value URLs) and returns Markdown for synthesis.
- **Negative-search / exclusion (PRD §8.4)** is **post-MVP / optional hardening** — out of
  the first implementation roadmap (see §7).

---

## 3. Phase Map (dependency order)

Ordered around a first `prowl search` vertical slice. The slice target (Phase 6) is:
SearXNG snippets → Qwen synthesis → rendered Pi output. Everything before it de-risks
the unknowns (exports resolution, the real Pi registration API, the model endpoint).

| # | Phase | Tag | Depends on |
| --- | --- | --- | --- |
| 0 | Project hygiene: `exports` maps + workspace install | **ship** | — |
| 1 | Core contracts required for `search` (define now; tenant/persist deferred) | **arch** | #0 |
| 2 | Minimal Pi extension registration spike | **build** | #0 |
| 3 | `ModelPort` (Qwen via OpenAI SDK) | **build** | #0 |
| 4 | Minimal `PresenterPort` + thin SearXNG wiring package | **build** | #2, #3 |
| 5 | Core `search` pipeline (snippets-only) | **arch** | #1, #3, #4 |
| 6 | **First vertical slice demo** — `prowl search <topic>` | **build** | #5 |
| 7 | Firecrawl `--read` / evidence mode (bounded extraction) | **build** | #5 |
| 8 | Tests + end-to-end demo acceptance | **build** | #6, #7 |
| — | `query` / `chat` commands (later phases) | **build** (deferred) | after #6 stable |
| — | Negative-search / exclusion (post-MVP hardening) | **build** (optional) | after #8 |

> Phases 2, 3 are independent leaf adapters (parallelizable after #0). Phase 5 is the
> central architectural seam (hence **arch**) and gates the slice. Phase 1 defines the
> contracts `search` needs but **defers** `CatalogPort` impl + tenant-scoped `StoragePort`
> fs work — those move to a post-slice phase (see §6 note).

---

## 4. Phases

### Phase 0 — Project hygiene: `exports` maps + workspace install  ·  **ship**

**Goal / why now:** Both packages lack an `exports` map, so clean
`import ... from "prowl-core"` resolution (used throughout the plan) is not guaranteed.
Bun workspace is already installed, but the package entry points must be explicit before
any cross-package wiring.

**Files:**
- `packages/core/package.json` (MODIFY) — add `"exports": { ".": "./src/index.ts" }`;
  keep `peerDependencies`/`devDependencies` unchanged.
- `packages/pi/package.json` (MODIFY) — add `"exports": { ".": "./src/index.ts" }`.
- (No new files; `bun install` already done — re-run only to confirm resolution.)

**Dependencies:** none.

**Acceptance criteria:**
- From `packages/pi`, `bun -e "import('prowl-core').then(m=>console.log(Object.keys(m)))"`
  prints the exported type names without a resolver error.
- `bun install` exits 0.

**Out of scope:** any behavioral code, tests, Docker, deployment.

**Workflow tag:** **ship** (isolated config/glue after design is effectively settled).

---

### Phase 1 — Core contracts required for `search` (define now; tenant/persist deferred)  ·  **arch**

**Goal / why now:** Locks the port surface the `search` pipeline needs, but does **not**
block the first slice on filesystem persistence or tenant partitioning. v0.1 is local and
single-user, so we define the contracts now and defer the heavy `CatalogPort` + tenant-
scoped `StoragePort` *implementations* to after the demo (§6).

**Files:**
- `packages/core/src/ports.ts` (MODIFY) — for v0.1 `search`, the required ports already
  exist: `SearchPort`, `ModelPort`, `PresenterPort`, `ScrapePort`. **Add the `ScrapePort`
  type annotation** to `firecrawl-client`'s `scrape` separately (Phase 7). **Define the
  `CatalogPort` + `SiteEntry` interface and a **tenant-scoped `StoragePort` signature** in
  core now** (so the seam exists), but mark their fs implementation as deferred:
  ```ts
  // Contract defined now; fs impl deferred to post-slice phase.
  export interface SiteEntry {
    url: string;
    language?: string;
    category?: string;
    authorityTier?: "raw" | "commercial" | "authoritative";
  }
  export interface CatalogPort {
    getSites(tenantId: string): Promise<SiteEntry[]>;
    addSite(tenantId: string, site: SiteEntry): Promise<void>;
  }
  // Tenant-scoped from the start (PRD §11.2) — fs impl deferred.
  export interface StoragePort {
    get(tenantId: string, key: string): Promise<string | null>;
    set(tenantId: string, key: string, value: string): Promise<void>;
  }
  ```
- `packages/core/src/index.ts` (MODIFY) — add `CatalogPort`, `SiteEntry` to the
  `export type` block (alphabetical). (`StoragePort` already exported.)

**Dependencies:** #0.

**Acceptance criteria:**
- `bunx tsc --noEmit -p packages/core/tsconfig.json` passes (strict,
  `noUncheckedIndexedAccess`).
- `grep -n "CatalogPort\|tenantId" packages/core/src/ports.ts` shows both contracts with
  `tenantId` threaded on `StoragePort.get/set` + `CatalogPort.getSites/addSite`.
- **No fs implementation in this phase** — `grep -rn "node:fs\|Bun.file\|~/.prowl" packages/core`
  returns nothing (core stays I/O-free).

**Out of scope:** any adapter implementation of `CatalogPort`/`StoragePort`; orchestration;
the `search` composer (Phase 5).

**Workflow tag:** **arch** (contract + dependency-direction change across core; defines the
seam that future persistence plugs into without refactoring).

---

### Phase 2 — Minimal Pi extension registration spike  ·  **build**

**Goal / why now:** The Pi extension registration API (`registerCommand` / `present` /
`ask_user` host surface) is an **explicit unknown** (see Assumptions §8.1). A minimal
spike must verify it early — before the full presenter/user-prompt adapter and before the
main pipeline — proving a command can register, receive an argument, and render a message.
This de-risks every later Pi-facing phase.

**Files:**
- `packages/pi/src/index.ts` (MODIFY — replace stub) — register **one** command
  `/prowl search` whose handler takes the topic arg, calls `presenter.present("registered: " + topic)`
  (or the host's equivalent), and does **no** search/model work yet.
- A minimal presenter shim inline in `index.ts` (or `packages/pi/src/pi-spike.ts` NEW) that
  binds the host `present` surface discovered during the spike.
- Deploy copy to `~/.pi/agent/extensions/prowl/`.

**Dependencies:** #0.

**Acceptance criteria:**
- After deploy, `/prowl search "hello"` prints `registered: hello` (or equivalent) in a pi
  session — proving: registration works, arg parsing works, rendering works.
- `grep -n "registerCommand\|present" packages/pi/src/index.ts` confirms the registration
  + render call sites.
- `ls ~/.pi/agent/extensions/prowl/` shows the deployed adapter.

**Out of scope:** real `PresenterPort`/`UserPromptPort` adapter (Phase 4), SearXNG/model
wiring, pipeline (Phase 5).

**Workflow tag:** **build** (research/validation of an unknown API + implementation + review —
this is why it is **not** `ship` despite being small).

---

### Phase 3 — `ModelPort` (Qwen via OpenAI SDK)  ·  **build**

**Goal / why now:** The single model is needed by `PLAN` (query planning) and
`SYNTHESIZE` in the pipeline (Phase 5). Building it as an isolated adapter early keeps the
pipeline phase free of provider concerns.

**Files:**
- `packages/pi/src/model-client.ts` (NEW) —
  ```ts
  import OpenAI from "openai";
  import type { ModelPort } from "prowl-core";

  const client = new OpenAI({
    apiKey: process.env.PROWL_MODEL_API_KEY ?? "",
    baseURL: process.env.PROWL_MODEL_BASE_URL, // OpenAI-compatible (Venice/OpenRouter/etc.)
  });
  const MODEL = process.env.PROWL_MODEL ?? "qwen3.6-plus";

  export const modelClient: ModelPort = {
    async generate(prompt: string, opts?: Record<string, unknown>) {
      const res = await client.chat.completions.create({
        model: (opts?.model as string) ?? MODEL,
        messages: [{ role: "user", content: prompt }],
      });
      return res.choices[0]?.message?.content ?? "";
    },
  };
  ```
- `packages/pi/src/index.ts` (MODIFY) — export `modelClient`.
- `.env.example` (MODIFY) — add `PROWL_MODEL_API_KEY`, `PROWL_MODEL_BASE_URL`,
  `PROWL_MODEL` under a new `## Prowl model` section (per env-config conventions).

**Dependencies:** #0.

**Acceptance criteria:**
- `bunx tsc --noEmit -p packages/pi/tsconfig.json` passes.
- `modelClient.generate("ping")` returns a non-empty string when `PROWL_MODEL_API_KEY` +
  `PROWL_MODEL_BASE_URL` point at a live Qwen-compatible endpoint.
- `grep -n "modelClient" packages/pi/src/index.ts` confirms the export.

**Out of scope:** multi-model routing, DSPy, tiered model selection (v0.2+).

**Workflow tag:** **build** (adapter needing provider research + validation).

---

### Phase 4 — Minimal `PresenterPort` + thin SearXNG wiring package  ·  **build**

**Goal / why now:** Turns the Phase 2 spike into a reusable surface: a real (minimal)
`PresenterPort` adapter bound to the host render surface discovered in the spike, plus the
SearXNG client already exists — this phase wires it into the pi package export surface so
Phase 5's pipeline can inject it. Keeps `UserPromptPort` out for now (only needed by
`chat`/`REFLECT`, deferred).

**Files:**
- `packages/pi/src/pi-ports.ts` (NEW) — `presenterPort` (renders a chat message via the
  host render surface confirmed in Phase 2). Structural typing only; no pi-internal import.
- `packages/pi/src/index.ts` (MODIFY) — export `presenterPort` + `searxngClient` +
  `modelClient` (port objects the minimal search handler injects).
- `packages/pi/src/searxng-client.ts` (MODIFY, tiny) — annotate `searxngClient` with the
  `SearchPort` type (already structurally compliant) so injection is type-clean.

**Dependencies:** #2 (host surface known), #3 (model available).

**Acceptance criteria:**
- Type-checks under `packages/pi/tsconfig.json`.
- `presenterPort.present("x")` resolves inside a pi session (manual check).
- `grep -n "presenterPort\|searxngClient\|modelClient" packages/pi/src/index.ts` confirms
  the three exports.
- No pi-internal package import (only structural host typing).

**Out of scope:** `UserPromptPort` (deferred to `chat` phase); `StoragePort`/`CatalogPort`
impls (deferred per §6).

**Workflow tag:** **build** (adapter binding needing host-API validation).

---

### Phase 5 — Core `search` pipeline (snippets-only)  ·  **arch**

**Goal / why now:** The orchestration heart for the first slice — composes ports into the
`search` command per `PRD §2.1`: `PLAN → SCATTER → GATHER → SYNTHESIZE → PRESENT`.
Snippets-only (no Firecrawl). This is the central architectural seam (hence **arch**): it
owns the command flow + ranking/selection policy, lives entirely in core with zero I/O, and
gates the demo (Phase 6) and every later command.

**Files:**
- `packages/core/src/pipeline.ts` (NEW) — primitive functions used by `search`:
  - `plan(query, model)` → `PLAN` (returns a (re)formed query / query-set via `ModelPort`).
  - `scatter(search, querySet, engines?)` → `SCATTER` (calls `SearchPort`, returns
    `SearchResult[]`).
  - `gather(results)` → `GATHER` (dedupe/normalize `SearchResult[]`).
  - `synthesize(model, query, gathered)` → `SYNTHESIZE` (calls `ModelPort` with snippets).
  - `present(presenter, content)` → `PRESENT`.
- `packages/core/src/commands.ts` (NEW) — `search(input, deps)` composer:
  `PLAN → SCATTER → GATHER → SYNTHESIZE → PRESENT`. Accepts a `deps` object
  (`{ search: SearchPort; model: ModelPort; present: PresenterPort }`) — structural
  injection; adapters supply concretes at call time. No port implementation lives here.
- `packages/core/src/ranking.ts` (NEW) — snippet-level selection/dedup policy used by
  `GATHER`/`SYNTHESIZE` context building.
- `packages/core/src/index.ts` (MODIFY) — export `pipeline`, `commands` functions/types.

**Dependencies:** #1 (contracts), #3 (model available to type, core only types it).

**Acceptance criteria:**
- `bunx tsc --noEmit -p packages/core/tsconfig.json` passes with **zero** imports of
  `fetch`, `process`, pi, or any adapter (`grep -rn "fetch\|process.env" packages/core`
  returns nothing).
- A unit test (Phase 8) drives `search` with **mock** ports and asserts the primitive
  sequence `PLAN→SCATTER→GATHER→SYNTHESIZE→PRESENT` + output shape (summary + sources).
- `grep -n "export" packages/core/src/index.ts` includes the new composers.

**Out of scope:** Firecrawl wiring (Phase 7); `query`/`chat` composers (later phases);
Pi UI (Phase 4/6 adapter side).

**Workflow tag:** **arch** (subsystem boundary + orchestration owning core policy).

---

### Phase 6 — First vertical slice demo: `prowl search <topic>`  ·  **build**

**Goal / why now:** The milestone. Binds the minimal pi registration (Phase 2/4) to the
core `search` composer (Phase 5) with injected `searxngClient` + `modelClient` +
`presenterPort`, so a real command runs end-to-end:

```
/prowl search "magnesium orotate personal experiences"
  → SearXNG result snippets (Google/Bing/Marginalia/Yandex…)
  → Qwen synthesis with URLs (snippets-only, no Firecrawl)
  → rendered Pi output (summary + source links)
```

**Files:**
- `packages/pi/src/index.ts` (MODIFY) — `/prowl search` handler now calls the core
  `search()` composer, injecting `searxngClient`, `modelClient`, `presenterPort`.
  (Registration already proven in Phase 2; this phase fills in real behavior.)
- Deploy copy refreshed to `~/.pi/agent/extensions/prowl/`.

**Dependencies:** #5 (pipeline), #4 (ports wired), #2 (registration proven).

**Acceptance criteria:**
- `/prowl search "magnesium orotate personal experiences"` runs end-to-end in a pi
  session and prints a synthesized summary + 3–5 source URLs.
- **Zero** Firecrawl calls in this phase (snippets-only default).
- `grep -n "search(" packages/pi/src/index.ts` confirms the core composer is invoked
  with the three injected ports.

**Out of scope:** `--read` evidence mode (Phase 7); `query`/`chat` (later); negative
search (post-MVP).

**Workflow tag:** **build** (integration of composer + adapters + review loop).

---

### Phase 7 — Firecrawl `--read` / evidence mode (bounded extraction)  ·  **build**

**Goal / why now:** Adds the conditional extraction layer behind an **explicit user flag**
(`prowl search --read <topic>`), satisfying the PRD constraint that Firecrawl is never
default-on. Read mode selects a **bounded 3–5 diverse, high-value URLs** from the gathered
results and returns Markdown for synthesis. Snippets remain the default path.

**Files:**
- `packages/pi/src/firecrawl-client.ts` (MODIFY) — annotate `scrape` as satisfying
  `ScrapePort` (export `scrapePort: ScrapePort = { scrape }` or type the function).
- `packages/core/src/selection.ts` (NEW) — core policy: given `GATHER`ed results +
  budget `k` (default 3–5), select the bounded diverse set (e.g., top-k by authority
  tier with diversity across engines/domains). No adapter I/O here.
- `packages/core/src/pipeline.ts` (MODIFY) — add an `EXTRACT` step gated on
  `scrape?: ScrapePort` + `readMode` flag; invoked only when `--read` is set.
- `packages/core/src/commands.ts` (MODIFY) — `search` accepts `readMode: boolean`;
  when true, runs `EXTRACT` on the selected set before `SYNTHESIZE`.
- `packages/pi/src/index.ts` (MODIFY) — parse `--read` flag, pass `readMode` to `search`.

**Dependencies:** #5 (pipeline hook point).

**Acceptance criteria:**
- Default `search` (no flag) performs **zero** Firecrawl calls (snippets-only, per Phase 6).
- With `--read`, exactly `min(budget, gathered)` URLs are scraped (3–5), all other
  results use snippets; synthesis receives the Markdown.
- Type-check passes; core still imports no `fetch` (scrape stays in adapter).
- `grep -n "readMode\|--read\|scrapePort" packages/pi/src/index.ts packages/core/src/commands.ts`
  confirms the flag + wiring.

**Out of scope:** scraping every result; archive fallback; CAPTCHA tiering; `query`/`chat`.

**Workflow tag:** **build** (pipeline slice + adapter binding + validation).

---

### Phase 8 — Tests + end-to-end demo acceptance  ·  **build**

**Goal / why now:** Locks the vertical slice. Unit tests for the core `search` composer
(mock ports), adapter smoke tests against the live Docker stack, and a documented manual
demo script covering both default and `--read` modes.

**Files:**
- `packages/core/test/` (NEW) — mock-port tests for `search` asserting primitive order +
  output shape + (Phase 7) zero-scrape default vs. bounded `--read` extraction.
- `packages/pi/test/` (NEW) — live smoke test: `searxngClient.search` returns ≥1 result;
  `scrape` returns markdown for one selected URL; `modelClient.generate` returns non-empty.
- `docs/planning/v0.1-demo.md` (NEW, optional) — manual demo walkthrough (§5).

**Dependencies:** #6, #7.

**Acceptance criteria:**
- `bun test` passes in both packages.
- `bunx tsc --noEmit` passes repo-wide.
- Manual: `/prowl search "<topic>"` → summary + sources; `/prowl search --read "<topic>"`
  → summary + Markdown-evidenced sources.

**Out of scope:** CI pipeline, coverage gates, performance benchmarking.

**Workflow tag:** **build** (validation + review loop).

---

## 5. Recommendations & Demo

### Recommended first implementation phases
**Phase 0** (ship, 5 min) → **Phase 1** (arch, contract-only) → **Phase 2** (build, Pi
spike) → **Phase 3** (build, ModelPort) → **Phase 4** (build, PresenterPort + wiring) →
**Phase 5** (arch, pipeline) → **Phase 6** (build, first demo). This is the shortest path
to a working `prowl search`.

### Smallest user-visible end-to-end demo (Phase 6)
```
/prowl search "magnesium orotate personal experiences"
  → SearXNG result snippets
  → Qwen synthesis with URLs
  → rendered Pi output
```
No `query`/`chat` required for this demo. `--read` evidence mode (Phase 7) is an
additive flag on top of the same command.

### Later phases (after the slice is stable)
- `query` / `chat` commands — require `StoragePort` (last-results context) and
  `UserPromptPort` (`REFLECT`/`ask_user`). Built after #6 stabilizes.
- `CatalogPort` + tenant-scoped `StoragePort` **filesystem implementations** — deferred
  from Phase 1 to a post-slice phase (see §6).

---

## 6. Deferred-from-Phase-1 Note

Phase 1 **defines** `CatalogPort` + tenant-scoped `StoragePort` in core but **defers their
filesystem implementations**. Reason: v0.1 is local and single-user; the first vertical
slice (Phase 6) needs only `SearchPort` + `ModelPort` + `PresenterPort` (all already
working or built in Phases 2–4). Filesystem persistence becomes necessary only when:
- `query`/`chat` need last-results context → `StoragePort` impl, **and**
- negative-search / catalog growth needs site storage → `CatalogPort` impl.

Those impls are scheduled as **post-slice phases**, not blockers for the first demo. This
keeps the tenant seam in the contract (no future refactor) without delaying usable search.

---

## 7. Out of First Roadmap (post-MVP / optional)

**Negative-search / exclusion (PRD §8.4).** Interesting product logic, but explicitly **out
of the first implementation roadmap**. Rationale:
- Not required to prove the first v0.1 vertical slice (snippets → synthesis → output).
- Adds classification / whitelist / global-vs-topic exclusion policy decisions that can
  stall usable search.
- Becomes a **post-MVP hardening phase** once `search` is stable and `CatalogPort` exists.

Tracked as a future phase: core policy (`buildExclusion` querying SEO engines via
`SearchPort`, post-filter + `-site:` dork builder) + `CatalogPort` seed list + composer
hook. Whitelist (wikipedia/pubmed/arxiv) survives exclusion.

---

## 8. Assumptions & Open Questions (resolve before implementation)

1. **Pi extension registration API.** The exact `registerCommand` / `present` / `ask_user`
   host-surface signatures are assumed from `.rpiv/guidance/packages/pi/src/architecture.md`.
   **Must confirm** against the live pi extension SDK before Phase 2 & 4 — the host API
   shape determines the spike (Phase 2) and `pi-ports.ts` (Phase 4). This is the explicit
   unknown that makes Phase 2 a **build**, not a ship.
2. **Model endpoint & key.** Qwen assumed reachable via an OpenAI-compatible base URL
   (`PROWL_MODEL_BASE_URL`) with `PROWL_MODEL_API_KEY`. **Confirm** the exact model id
   (`qwen3.6-plus`?) and that the endpoint supports the chat-completions shape used in #3.
3. **`--read` trigger + selection.** Assumed flag `prowl search --read <topic>`; bounded
   set default 3–5, diverse across engines/domains, high authority tier. **Confirm** the
   flag name and the selection heuristic (authority-tier vs. user `ask_user` pick vs. top-k
   by snippet signal) for Phase 7.
4. **`PLAN` output shape (v0.1).** `plan()` assumed to return a (re)formed single query /
   small query-set for `SCATTER`. The full multi-language/dork variation generator
   (`dork-planner.ts`, 8–12 variations) is **out of v0.1 scope** per
   `docs/planning/prowl-versions.md` — confirm v0.1 `PLAN` is a single-language planning
   stub (Qwen reforms the user query), not the multi-language emitter.
5. **Build/run tooling.** Assumed Bun (`bun test`, `bunx tsc`). **Confirm** `tsc` is the
   intended type-check (peer dep `typescript@^7`) vs. another driver, and that `bun test` is
   the test runner.
6. **Deploy mechanism.** Assumed a copy step from `packages/pi` →
   `~/.pi/agent/extensions/prowl/`. **Confirm** the pi extension loader expects source `.ts`
   directly or a build artifact, and whether the `exports` map (Phase 0) satisfies it.
7. **PresenterPort host surface.** Assumed the spike (Phase 2) discovers a `present(message)`
   host call. **Confirm** whether rendering is a function, a returned value, or a streamed
   message — affects `pi-ports.ts` (Phase 4) shape.
